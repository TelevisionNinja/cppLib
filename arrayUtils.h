#include <vector>

namespace tvnj {
    template <typename type>
    bool hasSubArrInOrder(std::vector<type> arr, std::vector<type> subArr);

    template <typename type>
    bool hasSubArr(std::vector<type> arr, std::vector<type> subArr);
}
